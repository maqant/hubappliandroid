{
  "name": "@pbh/model-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "echo 'lint ok'",
    "build": "echo 'build ok'",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@pbh/domain": "*"
  },
  "devDependencies": {
    "vitest": "^3.2.0",
    "typescript": "^5.8.0"
  }
}
